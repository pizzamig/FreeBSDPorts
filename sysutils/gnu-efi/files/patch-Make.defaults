--- Make.defaults.orig	2014-08-08 21:27:21.000000000 +0200
+++ Make.defaults	2014-10-08 10:29:08.322465900 +0200
@@ -45,10 +45,11 @@
 # Where to install the package. GNU-EFI will create and access
 # lib and include under the root
 #
-INSTALLROOT  := /
+INSTALLROOT  := GNUEFISTAGEDIR
 PREFIX       := /usr/local
 LIBDIR 	     := $(PREFIX)/lib
 INSTALL	     := install
+prefix       := /usr/local/bin/
 
 # Compilation tools
 HOSTCC       := $(prefix)gcc
